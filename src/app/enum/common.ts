/* eslint-disable no-unused-vars */
export enum Role {
  CLIENT = "CLIENT",
  ADMIN = "ADMIN",
}

export enum Status {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
}

export enum Gender {
  MALE = "MALE",
  FEMALE = "FEMALE",
}
